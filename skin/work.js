// /skins/minimalist.js

const minimalistSkin = {
    // 字體
    '--font-family': "system-ui, -apple-system, BlinkMacSystemFont, 'Helvetica Neue', 'sans-serif'",

    // 背景色
    '--bg-main': '#ffffff',          // 主背景 (頁面底部)
    '--bg-panel': '#ffffff',         // 面板背景 (如左右側欄)
    '--bg-content': '#ffffff',       // 內容區域背景 (如倉庫網格)

    // 文字顏色
    '--text-primary': '#000000',     // 主要文字 (標題、重要內容)
    '--text-secondary': '#555555',   // 次要文字 (描述、輔助資訊)
    '--text-on-accent': '#ffffff',   // 在強調色背景上的文字 (如按鈕)

    // 邊框顏色
    '--border-color': '#eeeeee',     // 主要邊框 (如倉庫格子)
    '--border-color-heavy': '#bbbbbb',// 較深的邊框 (如倉庫外框)

    // 強調色與互動元素
    '--accent-color': '#000000',     // 強調色 (按鈕、連結)
    '--accent-color-hover': '#333333',// 強調色懸停

    // 頁籤 (Tab) 顏色
    '--tab-active-bg': 'var(--bg-content)', // 啟用中頁籤背景
    '--tab-inactive-bg': '#f5f5f5',  // 未啟用頁籤背景
    '--tab-inactive-hover-bg': '#e0e0e0', // 未啟用頁籤懸停背景
};
